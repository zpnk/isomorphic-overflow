var React = require('react')

var About = React.createClass({

  render: function() {
    return <p>A simple isomorphic app built with React, React-Router, Browserify and Express.</p>
  }

})

module.exports = About
